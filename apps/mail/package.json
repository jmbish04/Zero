{
  "name": "@zero/mail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "react-router dev",
    "build": "react-router build",
    "start": "wrangler df --port 3000 --show-interactive-dev-session=false",
    "types": "wrangler types",
    "deploy": "wrangler deploy --name zero-mail",
    "lint": "eslint .",
    "machine-translate": "inlang machine translate --project project.inlang"
  }
}
